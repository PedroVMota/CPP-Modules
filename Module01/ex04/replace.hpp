#ifndef REPLACE_HPP
#define REPLACE_HPP

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

void replace_content(std::string &content, const std::string &to_find, const std::string &to_replace);

#endif